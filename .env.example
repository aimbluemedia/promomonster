# Postgres connection string. Optional in development — without it, waitlist
# signups are appended to .data/waitlist.jsonl so the site runs with zero setup.
# Required in production.
DATABASE_URL="postgresql://user:password@host:5432/promomonster"
